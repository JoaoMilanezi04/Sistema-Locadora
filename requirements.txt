# Dependências para o Sistema de Locadora de Veículos
# Este projeto usa principalmente bibliotecas padrão do Python
# tkinter - Interface gráfica (incluída no Python)
# sqlite3 - Banco de dados (incluída no Python)
# datetime - Manipulação de datas (incluída no Python)
# re - Expressões regulares (incluída no Python)
# math - Funções matemáticas (incluída no Python)

# Para garantir compatibilidade, especificamos a versão mínima do Python
# Python >= 3.8
